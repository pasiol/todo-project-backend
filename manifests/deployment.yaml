apiVersion: apps/v1
kind: Deployment
metadata:
  name: todo-project-backend
  namespace: todo-project
spec:
  replicas: 2
  selector:
    matchLabels:
      app: todo-project-backend
  template:
    metadata:
      labels:
        app: todo-project-backend
    spec:
      volumes:
        - name: env-file
          configMap:
            name: todo-db-host
            items:
              - key: env.properties
                path: ".env"
      containers:
        - name: todo-project-backend
          image: pasiol/todo-project-backend@sha256:5d8323d27d0a3f235172c6fb2c47494c5d8e8c061196d9cbb18a25d2cb037105
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres
                  key: POSTGRES_USER
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres
                  key: POSTGRES_PASSWORD
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: postgres
                  key: POSTGRES_DB
            - name: NATS_URL
              value: nats://my-nats:4222
          volumeMounts:
            - mountPath: "/var/app/.env"
              name: env-file
              subPath: ".env"
          readinessProbe:
            initialDelaySeconds: 30
            periodSeconds: 10
            httpGet:
              path: /health
              port: 8888
          resources:
            limits:
              cpu: "10m"
              memory: "20Mi"